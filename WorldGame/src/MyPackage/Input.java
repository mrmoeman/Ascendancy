package MyPackage;
import org.lwjgl.input.Keyboard;


class Input {

	public static boolean GetQ(){
		
		 if(Keyboard.isKeyDown(Keyboard.KEY_Q)) {       // Exit if Escape is pressed
	            return true;
	        }
		 else { return false;}
	
	}
	
	public static boolean GetE(){
		
		 if(Keyboard.isKeyDown(Keyboard.KEY_E)) {       // Exit if Escape is pressed
	            return true;
	        }
		 else { return false;}
	
	}
	
	public static boolean GetG(){
		
		 if(Keyboard.isKeyDown(Keyboard.KEY_G)) {       // Exit if Escape is pressed
	            return true;
	        }
		 else { return false;}
	
	}
	
	public static boolean GetW(){
		
		 if(Keyboard.isKeyDown(Keyboard.KEY_W)) {       // Exit if Escape is pressed
	            return true;
	        }
		 else { return false;}
	
	}
	
	public static boolean GetS(){
		
		 if(Keyboard.isKeyDown(Keyboard.KEY_S)) {       // Exit if Escape is pressed
	            return true;
	        }
		 else { return false;}
	
	}
	
	public static boolean GetR(){
		
		 if(Keyboard.isKeyDown(Keyboard.KEY_R)) {       // Exit if Escape is pressed
	            return true;
	        }
		 else { return false;}
	
	}
	
	public static boolean GetD(){
		
		 if(Keyboard.isKeyDown(Keyboard.KEY_D)) {       // Exit if Escape is pressed
	            return true;
	        }
		 else { return false;}
	
	}
	
	public static boolean GetA(){
		
		 if(Keyboard.isKeyDown(Keyboard.KEY_A)) {       // Exit if Escape is pressed
	            return true;
	        }
		 else { return false;}
	
	}
	
	public static boolean GetZ(){
		
		 if(Keyboard.isKeyDown(Keyboard.KEY_Z)) {       // Exit if Escape is pressed
	            return true;
	        }
		 else { return false;}
	
	}
	
	
	public static boolean GetSpace(){
		
		 if(Keyboard.isKeyDown(Keyboard.KEY_SPACE)) {       // Exit if Escape is pressed
	            return true;
	        }
		 else { return false;}
	
	}
	
	
}
